test()->
    A = #<eos_etsobj>{
        uho = method()->io.format("iiotoko\n",[])
        hello = method(X)->io.format("hello ~s\n",[X])
        set = method(X)->
                This.a := X
                This.b := X*2
        add = method()->
                This.a + This.b
    }
    
    io.format("~p\n",[A.hello])
    A.hello("world")
    A.uho()

    A.set(10)
    30 = A.add()
